# raw OCR is re-fetched from the Internet Archive on every build
data/raw/
__pycache__/
.claude/
.netlify/
# the search index: built by tools/build_search.py, on Netlify as the build command
pagefind/
